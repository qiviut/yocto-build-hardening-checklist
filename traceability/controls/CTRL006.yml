active: true
control_status: partial
control_type: operational
derived: false
evidence_needed: |
  Capture an effective environment and mount inventory from the release worker;
  assert protected names, sockets, and files are absent.
header: |
  Minimal worker environment
implementation: |
  Start each worker from an explicitly cleared environment, then add only the
  values required for the task. Remove SSH agents, cloud tokens, proxy
  credentials, broad HOME, uncontrolled PATH entries, and credential helpers
  from release workers; do not treat FETCH_EXPORT_VARS as a complete allow-list.
level: 3.6
links:
- RISK006: _TZQX-KbCBTMu_GCvVoj8owIAuHA1mTZ5aN4EHmFIrM=
normative: true
operational_mitigation: |
  Place any source-intake credentials in a separate short-lived intake lane and
  rotate them after suspected worker contamination.
record_type: control
ref: ''
reviewed: NJdGEdPSDZUpPOxYq7A2rcza_5whJWgKuHaM7ON0LC8=
text: |
  Build workers shall start with an explicitly cleared environment and controlled
  helper configuration, with no ambient secrets or developer home files. The
  task-specific environment must be smaller than the worker's inherited host
  environment.
